spring:
  datasource:
    url: jdbc:postgresql://<YOUR_POSTGRESQL_URL>:5432/<DATABASE_NAME>
    username: <USERNAME>
    password: <PASSWORD>
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
